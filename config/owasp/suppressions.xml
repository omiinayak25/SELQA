<?xml version="1.0" encoding="UTF-8"?>
<!--
  OWASP Dependency-Check suppressions.
  Each suppression MUST carry a justification and, ideally, a review date.
  Test-automation dependencies are not shipped to production, but we still
  track CVEs so an upgrade path is visible. Add entries deliberately.
-->
<suppressions xmlns="https://jeremylong.github.io/DependencyCheck/dependency-suppression.1.3.xsd">

    <!-- Example template (kept commented):
    <suppress>
        <notes>Transitive only in tests; no runtime exposure. Reviewed 2026-06-29.</notes>
        <packageUrl regex="true">^pkg:maven/com\.example/.*@.*$</packageUrl>
        <cve>CVE-0000-00000</cve>
    </suppress>
    -->

</suppressions>
