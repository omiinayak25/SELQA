<?xml version="1.0" encoding="UTF-8"?>
<!--
  OmiinQA centralized logging configuration.
  - Console: human-readable, colored, INFO+.
  - RollingFile: full DEBUG trace, rolled daily / at 10MB, 30-file history.
  - ThreadContext (%X{testName}, %X{thread}) lets parallel runs be untangled.
-->
<Configuration status="WARN">
    <Properties>
        <Property name="LOG_PATTERN">
            %d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %-5level %X{testName} %logger{36} - %msg%n
        </Property>
        <Property name="CONSOLE_PATTERN">
            %highlight{%d{HH:mm:ss.SSS} %-5level} [%style{%t}{cyan}] %style{%logger{1}}{magenta} - %msg%n
        </Property>
        <Property name="LOG_DIR">logs</Property>
    </Properties>

    <Appenders>
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="${CONSOLE_PATTERN}"/>
        </Console>

        <RollingFile name="FileAppender"
                     fileName="${LOG_DIR}/omiinqa.log"
                     filePattern="${LOG_DIR}/archive/omiinqa-%d{yyyy-MM-dd}-%i.log.gz">
            <PatternLayout pattern="${LOG_PATTERN}"/>
            <Policies>
                <TimeBasedTriggeringPolicy/>
                <SizeBasedTriggeringPolicy size="10 MB"/>
            </Policies>
            <DefaultRolloverStrategy max="30"/>
        </RollingFile>
    </Appenders>

    <Loggers>
        <!-- Framework packages at DEBUG -->
        <Logger name="com.omiinqa" level="DEBUG" additivity="false">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="FileAppender"/>
        </Logger>

        <!-- Silence noisy third parties -->
        <Logger name="org.apache.http" level="WARN"/>
        <Logger name="io.netty" level="WARN"/>
        <Logger name="org.openqa.selenium" level="WARN"/>

        <Root level="INFO">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="FileAppender"/>
        </Root>
    </Loggers>
</Configuration>
